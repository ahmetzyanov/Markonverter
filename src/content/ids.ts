// DOM ids shared between the panel (app.ts) and the delivery-selector modules,
// mostly so page scans can exclude our own UI.
export const PANEL_ID = "markonverter-panel-root";
export const MENU_ASSIST_ID = "markonverter-ozon-delivery-assist";
